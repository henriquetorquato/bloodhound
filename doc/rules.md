# Rules

## Rule support

- Resource level
    - matches
- Content level
    - matches
    - element
    - attribute filter

## Future support

- Filter out (remove resource if matches)
- Have a response level category